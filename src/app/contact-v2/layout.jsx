import '../../assets/css/home-v2.css';

// Loads V2 tokens (shared with /home, /landing-v2) without affecting
// other routes.
export default function ContactV2Layout({ children }) {
  return children;
}
